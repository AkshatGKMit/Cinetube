import Colors, { Dark, Light } from '@constants/colors';

const Theme: Theme = {
  light: {
    screenBG: Colors.white.slightWhite,
    statusBar: Light.c1,
  },
  dark: {
    screenBG: Colors.black.slightBlack,
    statusBar: Dark.c1,
  },
};

export default Theme;
